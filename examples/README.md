# Examples

Each subdirectory is one self-contained, runnable increment — an agent, a skill,
or an MCP integration — with its own README explaining what it is, how to run it,
and which research note it came from. If it doesn't run, it doesn't belong here.

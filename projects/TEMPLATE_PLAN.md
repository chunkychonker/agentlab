# <Project name>

## Goal

<One paragraph: what this becomes, and for whom. This is the thing every
cycle's work should visibly move toward.>

## Scope

In scope: <...>
Out of scope: <...> — say so explicitly; a milestone outside this list should
not get picked up without first amending this section.

## Milestones

Ordered; the researcher picks the topmost `not-started` item each cycle (or
resumes the topmost `in-progress`/`blocked-but-now-unblocked` one — never
skip ahead of an unfinished predecessor without saying why in the decisions
log). Status values: `not-started` / `in-progress` / `blocked: <reason>` /
`done <PR#>`.

1. [not-started] <milestone>
2. [not-started] <milestone>

## Decisions log

Append-only — never edit or delete an entry. To change a prior decision, add
a new entry that supersedes it and says why. A cycle's work must not
contradict an entry here without adding one.

- YYYY-MM-DD: <decision> — <rationale>

## Current state

Replaced (not appended) by every cycle that touches this project: what
exists right now, what's next, and any known issue a future cycle needs to
know about before it starts.

<narrative>
